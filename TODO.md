# TODO

Planned and queued work lives in `specs/` — see [`specs/index.md`](specs/index.md).

New items go directly into the relevant spec file (or a new one if no existing
spec fits). Do NOT accumulate a queue here; the index is the single list.
