# Plan 01-02: Protected Events Table → Terminal Style

## Objective
Convert the Protected Events page table from HTML `<table>` to pre-formatted monospace terminal output.

## Tasks

### Task 1: Rewrite protected-events-table.tsx
- Remove ShadTable imports
- Convert Metric cards to inline `:: LABEL value` stats line
- Convert Badge components to plain `[TEXT]` inline text (e.g. `[DRYRUN]`, `[ESTIMATE]`, `[BID LOW]`, `[UPTIME]`, `[COMM]`)
- Funder: `[MARINADE]` or `[VALIDATOR]`
- Render as pre-formatted monospace with pipe separators
- Columns: EPOCH (6w), VALIDATOR (22w), SETTLEMENT (12w), REASON (20w), FUNDER (12w)
- Keep filters (validator search + epoch range) but style inputs as terminal: `> ` prefix, bordered
- Sorting still works via column header click

### Success Criteria
- [ ] No HTML table elements
- [ ] Pre-formatted monospace with pipe separators
- [ ] Filters work
- [ ] Badges are plain text brackets
- [ ] Builds without errors

## Files to Modify
- src/components/protected-events-table/protected-events-table.tsx
