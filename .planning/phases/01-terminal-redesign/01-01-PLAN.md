# Plan 01-01: Validator Bonds Table → Terminal Style

## Objective
Convert the Validator Bonds page table from HTML `<table>` to pre-formatted monospace terminal output, matching the SAM table style.

## Tasks

### Task 1: Rewrite validator-bonds-table.tsx
- Remove all imports of ShadTable/TableHeader/TableBody/TableRow/TableHead/TableCell
- Remove Metric cards — convert to inline `:: LABEL value` stats line
- Render table as `<div>` with fixed-width columns using `pad()`/`rpad()` helpers
- Use `│` separators and `─` header line
- Columns: VALIDATOR (22w), BOND BAL (12w), MAX WANTED (12w), COMM (16w), MRN STAKE (12w), EFF COST (10w)
- Sortable: clicking column header cycles sort (show ▲/▼ in header text)
- Keep HelpTip `[?]` markers

### Success Criteria
- [ ] No HTML table elements used
- [ ] Monospace pre-formatted output with pipe separators
- [ ] Sorting still works
- [ ] Stats shown as inline terminal text
- [ ] Builds without errors

## Files to Modify
- src/components/validator-bonds-table/validator-bonds-table.tsx
