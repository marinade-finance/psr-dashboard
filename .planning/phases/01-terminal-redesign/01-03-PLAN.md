# Plan 01-03: Validator Detail Panel → Terminal Style

## Objective
Convert the validator detail slide-out panel to look like a terminal info dump — like `systemctl status` or `docker inspect` output.

## Tasks

### Task 1: Rewrite validator-detail.tsx
- Keep Sheet/SheetContent wrapper for slide-out behavior
- Header: `═══ VALIDATOR: name (vote_account) ═══` style
- Sections use `┌──── SECTION NAME ────┐` / `│` / `└────────────────────┘` ASCII boxes
- "Why Rank" factors: list with `│ ok  Max APY: 8.02% (+1.23% above cutoff)` style
- APY Composition: vertical list with `│` prefix
- Simulation form: labels with `> ` prefix, inputs stay functional
- Button: `[ Run Simulation ]` text style
- Bond Health: ASCII bar `[████░░░░░░] 65.2%`
- Stake Overview: key-value pairs with dots padding `Active Stake .... 123,456 ◎`

### Success Criteria
- [ ] Looks like terminal `cat` output
- [ ] Simulation still functional
- [ ] ASCII box drawing for sections
- [ ] Dot-leader key-value pairs
- [ ] Builds without errors

## Files to Modify
- src/components/validator-detail/validator-detail.tsx
