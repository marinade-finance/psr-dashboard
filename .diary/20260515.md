# 2026-05-15 — UX walkthrough as a 13-year-old, and two new skills

## What we did

Ran a full UX walkthrough of the PSR Dashboard with a fresh-novice
persona — a bright 13-year-old who can read the manual — and built
the methodology + reusable tooling on the same day.

End-to-end pipeline:

1. Captured the user's verbatim request in `docs/ux-13yo/instructions.md`
   so the brief survives context compression.
2. Spawned a research subagent on **frontend UX testing best practices**,
   targeting solo-evaluator + persona-driven methods. Output:
   `docs/ux-13yo/research-uxtest.md`.
3. Sent the research doc to `oracle` (codex CLI) with adversarial
   framing — got back a brutal critique. Oracle caught real factual
   errors:
   - Bailey & Wolfson 87/46 % was overclaimed (only applies to live
     task flows, not solo screenshot tests)
   - Hertzum & Jacobsen "22 %" was a misread — the range is 5–65 %
   - Cockton & Woolrych ">50 %" was overclaimed — the cited study
     reports 65 % in one specific setup
   - The 13yo persona is blind to financial judgement on a finance
     dashboard — the methodology was missing the most consequential
     dimension
4. Folded the critique into the methodology. Saved trail at
   `docs/ux-13yo/oracle-critique.md`.
5. Wrote `eye-13y` skill (UX walkthrough, 13yo lens) and `create`
   skill (research → oracle → mint a SKILL.md). Both at
   `~/.claude/skills/`.
6. Dogfooded `eye-13y` on the dashboard: 11 screens (SAM basic/expert,
   Bonds basic/expert, Events basic/expert, Docs basic/expert,
   validator detail, simulation, navigation).

## What we found in the dashboard

- **2 blockers, 93 frictions** across 11 reports.
- **Headline blocker:** validator-detail panel shows "Reserve: Fully
  covered" (green) next to "Bond below minimum — 7 SOL required"
  (orange) on the same validator. `GUIDE.md` explains the
  minimum-vs-ideal distinction, but the panel doesn't. Persona can't
  reconcile.
- **Headline delight:** simulation mode's banner —
  "SIMULATION MODE — WHAT-IF NUMBERS, NOT LIVE (N VALIDATOR MODIFIED)"
  — is the dashboard's single best irreversibility microcopy. The
  persona's #1 fear (real vs pretend money) is resolved on first
  glance. Worth porting the pattern elsewhere.
- **Pattern:** the dashboard is *learnable* (manual closes most gaps)
  but not yet *legible* (chrome doesn't link the manual from where
  confusion arises). Cleanup is mostly a labelling and discoverability
  project, not a redesign.

Full summary: `docs/ux-13yo/summary.md`. Per-screen reports under
`docs/ux-13yo/reports/`, evidence screenshots under
`docs/ux-13yo/reports/screens/`.

## What the pipeline taught us about itself

The dogfood surfaced operational flaws in the brand-new skills,
within hours of writing them. Sent both skills back to oracle for a
second adversarial pass — critique at
`docs/ux-13yo/oracle-skills-critique.md`. Verdict: pipeline is
directionally useful but not yet a reliable skill-minting process.
Specific failure modes:

- 4 parallel walkthrough subagents hit usage caps before writing
  reports. Screenshots survived; markdown didn't. Salvage loop (2
  lighter writer-only subagents) recovered the data.
- The "write the report at the end" pattern is fragile under
  interruption. Should be "create file first, append per step".
- `create`'s dogfood step is one line — exactly the kind of guard rail
  that gets skimmed past.
- Subagents may be sandbox-blocked from writing `~/.claude/skills/`;
  main thread must own the final SKILL.md write.
- `blocker / friction` is still severity ranking, just coarser —
  some reports smuggled `small / major / minor` back in despite the
  NEVER rule.

Proposed updates for both skills tracked in
`docs/ux-13yo/skill-updates.md`.

## Skills minted today

- `~/.claude/skills/eye-13y/SKILL.md` — solo UX walkthrough through
  a 13yo novice + manual-reader lens, with finance-judgement overlay.
- `~/.claude/skills/create/SKILL.md` — research → adversarial oracle
  critique → mint a SKILL.md. Used to produce `eye-13y` itself.
- Both names follow the user's preferred style: punchy compounds that
  state the point at a glance (mimicking `last30days`).
  See `feedback_skill_naming.md` in project memory.

## v2 of both skills (later same day)

Sent the v1 skills back to oracle for adversarial second pass. Critique
(`docs/ux-13yo/oracle-skills-critique.md`) said: directionally useful
but "not fit for purpose as-is" — operational hardening between "good
idea" and "reusable skill" is the gap.

Applied changes:

- `create` renamed → `scavenge`. The metaphor is honest: we don't
  invent, we scavenge known best practices (NN/g, Norman, Krug,
  GOV.UK, ...) and assemble what survives oracle.
- `scavenge` broadened beyond SKILL.md — now produces any of seven
  durable shapes: skill, agent, guideline, runbook, checklist, review
  template, spec. Step 1 picks the shape by REUSE + INVOCATION
  pattern; step 5 branches.
- `scavenge` got **critique checkpoints at every step**, not just at
  step 3. Oracle threads through (light sanity at step 1, deep
  adversarial at step 3, post-write check at step 5, dogfood-as-
  critique at step 6).
- `eye-13y` renamed → `eye-13yo` (matches `ux-13yo` docs dir).
- `eye-13yo`: `blocker / friction` tags removed (they were still
  severity ranking and dogfood proved synonyms leaked back in).
  Replaced with `cite / inference` — **evidence type, not severity**.
- `eye-13yo`: scaffold-report-file-first promoted to setup; subagents
  append per step, never write only at the end (the failure mode
  that lost work today).
- `eye-13yo`: cold findings and post-manual findings now hard-separated
  as distinct headings — retroactive reclassification destroyed
  evidence in v1.
- `eye-13yo`: finance overlay is automatic on any money-bearing screen
  (trigger list), not "if you notice". Six fixed lines, fixed order.
- Both skills: default 1–2 subagents, not ≤4. Explicit agent-browser
  invocation via Skill tool. Salvage rule for interrupted subagents.

Proposals trail: `docs/ux-13yo/skill-updates.md` (marked applied at
top).

## Open thread

Diary entries weren't being written automatically — user flagged this.
A Stop hook firing `python3 ~/.claude/hooks/diary.py` (similar to the
commit-check hook) would close the loop. Separate session.

---

# Session 2 (afternoon) — copy/visual/feature sweep, multi-subagent

## 16:10

Bug: `sam.ts` had an unresolved merge conflict
(`selectExpectedStakeChangeBreakdown`). It was the ROOT CAUSE of the
phantom "Property 'tr'/'svg' does not exist on JSX.IntrinsicElements"
diagnostics flooding every .tsx — tsserver chokes when an imported
module won't parse. Resolved (kept the named-type form). Lesson: a wall
of intrinsic-JSX errors usually means one broken import, not React types.

## 16:40

Decision: scoped the no-parens rule (was absolute, over-applied to
`(5ep)`). Now: NEVER in prose/help/titles; tolerated in deep technical
docs; EXCEPTION for compact UI tokens `(5ep)`/`(capped)`/`(+2)`.
Codified in `~/.claude/skills/writing/SKILL.md` + global CLAUDE.md.
`(Nep)` runway revert kept per user. Reported to user verbatim.

## 16:55

Decision: "Winning threshold" → "Winning APY threshold" on the Max APY
Composition card. It is an APY rebuilt at THIS validator's commission
profile (selectWinningApyForValidator), not a bid and not the 7% SAM
commission cap — user asked which; disambiguated in label + guide.

## 17:05

Discovery/decision: top-up advice must CEIL-round. Rounding a required
top-up down tells the validator to deposit less than needed — advice is
then wrong. New `topUp()` helper (Math.ceil, min 1 SOL); `payCta`/
`stakeCta` + the `<1 SOL` token deleted. Buffer ON TOP is #38 (blocked
on user's buffer-% policy).

## 17:20

KEY DECISION (epoch meter): adversarial critique disproved the premise.
No "settlements prepared/finalized" signal exists in app data
(BondRecord.epoch is a snapshot epoch; PE only has DRYRUN/FACT/ESTIMATE).
Network epoch must come from the protected-events query (epochs=3), NOT
bonds (epochs=0 → empty stats). auctionEpoch==networkEpoch normally.
Shipped a reduced, honest meter per user's chosen scope. `0c5abaa4`.

## 17:35

User correction: Payments+Bidding merge must unify the TABLES, not just
stack cards. #36 had shipped 4 stacked cards; couldn't steer it
(SendMessage unavailable). Let it finish, then #41 truly unified into
`payments-merged.tsx` — RevRow deleted, rows deduped, one black total.
`8c5fdbb5`.

## 17:45

Bug fix: estimator now predicts FULL stake loss when
`bondBalanceSol < minBondBalanceSol` (SDK clipBondStakeCap → 0). Was
under-reporting; tip text already said it but the number didn't.
`8fac99c2`.

## 17:53

Open items: #37 CTA icon/severity redesign running (kill misleading ↗
— color=severity, icon=constraint). #38 top-up safety buffer blocked on
user buffer-% policy. #42 click-to-pin shipped but compile-verified
only — Radix Tooltip isn't built for clickable sticky content; the
pin/unpin interaction needs an in-browser eyeball.

## Workflow note

Ran ~12 subagents (parallel + serial) with strict exclusive
file-ownership per sub to avoid working-tree corruption from concurrent
writers. SendMessage is unavailable here — a running subagent cannot be
steered mid-flight; correct by letting it finish + a follow-up sub.
Stop-hook "uncommitted changes" fires on in-flight sub work — do NOT
commit another agent's half-done edits.

# Session 3 (evening) — CTA/bond visuals, tab re-think, logic bugs

## 18:10

Shipped CTA icon/severity split `2ae72b1e`: getTipStyle returns only
{color,bg}; new getTipIcon maps constraint→fixed glyph; directional
↗/↘/→ only for the real signed delta. Kills the misleading up-arrow on
losing/blocked rows. color=severity, icon=meaning — one precedence.

## 18:35

Bidding/Payments tab arc settled. User went merge → "truly merge tables"
→ "split again" → finally: NOT mechanical splitting, PURPOSE-driven.
Payments = how much you pay; Bidding = what to bid to get in & win
stake. Shipped `126960d2`/`5a74ba77` as two purpose-built tabs, shared
4-col RevRow restored, PMPE/SOL moved to section headers.

## 18:50

`f7084a63` concentration: rescaled bars cap-relative→absolute share so a
labeled cap tick is meaningful and over-cap entries overflow past it.
Same pattern reused for `63a99637` bond gauge — the old `100−util` bar
was always ≈full (util≈0 for everyone) and conveyed nothing; now
runway-vs-100ep, tier-colored, red critical tick, uniform 4px.

## 19:10

BUG fixed `0227b768`: soft "Top up to grow stake" tip fired even when
the validator was already RECEIVING stake next epoch (delta>0) — the
bond-CTA cascade ran before the delta>0 branch. Root: topUpToIdealKeep
sizes bond vs current stake; delta is the auction's redelegation
allocation — independent inputs, commonly disagree. Fix: guard soft
ideal CTA behind delta<=0. Critical-fee + watch-keep-stake stay ahead
of delta with evidence (inflow doesn't pay the fee or refill the bond).

## 20:00

KEY PROCESS LESSON: all commits in this repo carry the user's git
identity — subagent commits and the user's own hand-commits are
INDISTINGUISHABLE by `git author`. A clean coherent commit `edf5dc63`
[epoch-meter] appeared mid-session, outside any sub's brief. Did NOT
auto-revert (CLAUDE.md: investigate before overwriting possible user
work); evidence (clean, scoped, builds, later commits stack on it) said
user-authored; confirmed with user — it was theirs. Re-dispatched the
genuinely-undelivered #47 with an explicit scope-discipline preamble
(prior #47 produced no deliverable; "next-epoch" was ambiguous between
the epoch meter and the redelegation model).

## 20:05

Oracle (adversarial): same advisory rendered indigo (header tip,
urgency 'info') AND yellow (bond banner, bond-tone) — two uncoordinated
color taxonomies. Verdict: not defensible; the duplication is the real
smell; dedupe to one banner + one severity source (bond-health/warning
axis). Dispatched #49. oracle session 019e2ce8.
